# Official paid SDKs: Perplexity Search for discovery and native Google Gen AI
# for structured synthesis. No model gateway is used.
google-genai>=2.0,<3
perplexityai>=0.1,<1
pydantic>=2.7,<3
python-dotenv>=1.0
requests>=2.31
tenacity>=8.2

# Composio SDK for authoritative catalog depth plus the read-only Gemini Session
# agent. Keep the provider on the same minor line as core because their tool-call
# adapters evolve together.
composio>=0.17,<0.18; python_version >= "3.10"
composio-google>=0.17,<0.18; python_version >= "3.10"
